{
  "name": "meeting-plunger-backend",
  "version": "0.1.0",
  "description": "Meeting Plunger Backend - Python/FastAPI service",
  "private": true,
  "scripts": {
    "test": "pytest -v",
    "lint": "ruff check .",
    "format": "ruff format .",
    "migrate": "alembic upgrade head"
  }
}
